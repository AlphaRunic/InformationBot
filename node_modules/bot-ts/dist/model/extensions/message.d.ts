import { DMChannel, Message as DiscordJsMessage, TextChannel } from 'discord.js';
import { Command } from '..';
import { Client } from '.';
/**
 * Subclass of the Discord.js Message class.
 *
 * @see https://discord.js.org/#/docs/main/master/class/Message
 */
export declare class Message extends DiscordJsMessage {
    readonly channel: DMChannel | TextChannel;
    readonly data: object;
    readonly client: Client;
    /**
     * The command that was ran.
     */
    command?: Command;
    constructor(channel: DMChannel | TextChannel, data: object, client: Client);
}
