import { Guild, GuildMemberResolvable } from 'discord.js';
import { Maybe } from 'purify-ts/Maybe';
import { UserService } from '../services';
export declare type ArgumentType = 'user' | 'number' | 'string';
interface ArgumentResolverOptions {
    input: string;
    guild: Guild;
    userService: UserService;
}
declare type Resolvable<T> = (options: ArgumentResolverOptions) => Maybe<T>;
export declare const ArgumentTypeResolver: (key: ArgumentType) => Resolvable<GuildMemberResolvable> | Resolvable<number> | Resolvable<string>;
export {};
